import css from "./flip-clock2.module.css";

export function FlipClock2({initialTime, add = false}: {initialTime: number; add?: boolean;}) {
    return (
        <div className="flex flex-col gap-4">
        </div>
    );
}
